"""Test package for :mod:`nncg`, one module per source module.

``test_api`` / ``test_inner`` / ``test_krylov`` / ``test_solver`` mirror
``src/nncg/`` — the paper's numerical study as a test suite, plus the
planted-problem generators in :mod:`tests.problems`.
"""
