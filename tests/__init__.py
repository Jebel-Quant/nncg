"""Test suite of nncg: the paper's numerical study, plus tests/problems.py.

The ``__init__.py`` makes ``tests`` a package so the planted-problem
generators are importable as ``tests.problems`` — from the test modules, and
from later experiments and notebooks.
"""
